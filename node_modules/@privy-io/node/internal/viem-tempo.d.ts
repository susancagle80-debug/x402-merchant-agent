import type { Hex } from 'viem';
import { Transaction as ViemTempoTransaction } from 'viem/tempo';
import type { EthereumSignTransactionRpcInputParams } from "../resources.js";
export type TempoTransaction = (ViemTempoTransaction.TransactionRequestTempo | ViemTempoTransaction.TransactionSerializableTempo) & {
    chainId?: ViemTempoTransaction.TransactionSerializableTempo['chainId'];
    feePayerSignature?: ViemTempoTransaction.TransactionSerializableTempo['feePayerSignature'];
    from?: Hex;
};
type PrivyTempoTransaction = Extract<EthereumSignTransactionRpcInputParams['transaction'], {
    type: 118;
}>;
type TempoAccountFingerprint = {
    keyType?: string;
    source?: string;
};
/** Delegates Tempo transaction detection to viem's Tempo transaction helpers. */
export declare function isTempoTransaction(tx: object, account?: TempoAccountFingerprint): tx is TempoTransaction;
export declare function formatTempoTransaction(tempoTx: TempoTransaction): PrivyTempoTransaction;
export {};
//# sourceMappingURL=viem-tempo.d.ts.map