"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Wallets = void 0;
const tslib_1 = require("../../internal/tslib.js");
const resource_1 = require("../../core/resource.js");
const ActionsAPI = tslib_1.__importStar(require("./actions.js"));
const actions_1 = require("./actions.js");
const BalanceAPI = tslib_1.__importStar(require("./balance.js"));
const balance_1 = require("./balance.js");
const SwapAPI = tslib_1.__importStar(require("./swap.js"));
const swap_1 = require("./swap.js");
const TransactionsAPI = tslib_1.__importStar(require("./transactions.js"));
const transactions_1 = require("./transactions.js");
const EarnAPI = tslib_1.__importStar(require("./earn/earn.js"));
const earn_1 = require("./earn/earn.js");
const pagination_1 = require("../../core/pagination.js");
const headers_1 = require("../../internal/headers.js");
const path_1 = require("../../internal/utils/path.js");
class Wallets extends resource_1.APIResource {
    constructor() {
        super(...arguments);
        this.actions = new ActionsAPI.Actions(this._client);
        this._earn = new EarnAPI.Earn(this._client);
        this.transactions = new TransactionsAPI.Transactions(this._client);
        this.balance = new BalanceAPI.Balance(this._client);
        this.swap = new SwapAPI.Swap(this._client);
    }
    /**
     * Creates a new wallet on the requested chain and for the requested owner.
     *
     * @example
     * ```ts
     * const wallet = await client.wallets.create({
     *   chain_type: 'ethereum',
     * });
     * ```
     */
    create(params, options) {
        const { 'privy-idempotency-key': privyIdempotencyKey, ...body } = params;
        return this._client.post('/v1/wallets', {
            body,
            ...options,
            headers: (0, headers_1.buildHeaders)([
                { ...(privyIdempotencyKey != null ? { 'privy-idempotency-key': privyIdempotencyKey } : undefined) },
                options?.headers,
            ]),
        });
    }
    /**
     * Get all wallets in your app.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const wallet of client.wallets.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/v1/wallets', (pagination_1.Cursor), { query, ...options });
    }
    /**
     * Export a wallet's private key
     *
     * @example
     * ```ts
     * const walletExportResponseBody =
     *   await client.wallets._export('wallet_id', {
     *     encryption_type: 'HPKE',
     *     recipient_public_key:
     *       'BDAZLOIdTaPycEYkgG0MvCzbIKJLli/yWkAV5yCa9yOsZ4JsrLweA5MnP8YIiY4k/RRzC+APhhO+P+Hoz/rt7Go=',
     *   });
     * ```
     */
    _export(walletID, params, options) {
        const { 'privy-authorization-signature': privyAuthorizationSignature, 'privy-request-expiry': privyRequestExpiry, ...body } = params;
        return this._client.post((0, path_1.path) `/v1/wallets/${walletID}/export`, {
            body,
            ...options,
            headers: (0, headers_1.buildHeaders)([
                {
                    ...(privyAuthorizationSignature != null ?
                        { 'privy-authorization-signature': privyAuthorizationSignature }
                        : undefined),
                    ...(privyRequestExpiry != null ? { 'privy-request-expiry': privyRequestExpiry } : undefined),
                },
                options?.headers,
            ]),
        });
    }
    /**
     * Initialize a wallet import. Complete by submitting the import.
     *
     * @example
     * ```ts
     * const response = await client.wallets._initImport({
     *   address: 'address',
     *   chain_type: 'ethereum',
     *   encryption_type: 'HPKE',
     *   entropy_type: 'hd',
     *   index: 0,
     * });
     * ```
     */
    _initImport(body, options) {
        return this._client.post('/v1/wallets/import/init', { body, ...options });
    }
    /**
     * Sign a message with a wallet by wallet ID.
     *
     * @example
     * ```ts
     * const rawSignResponse = await client.wallets._rawSign(
     *   'wallet_id',
     *   {
     *     params: {
     *       bytes:
     *         '',
     *       encoding: 'hex',
     *       hash_function: 'sha256',
     *     },
     *   },
     * );
     * ```
     */
    _rawSign(walletID, params, options) {
        const { 'privy-authorization-signature': privyAuthorizationSignature, 'privy-idempotency-key': privyIdempotencyKey, 'privy-request-expiry': privyRequestExpiry, ...body } = params;
        return this._client.post((0, path_1.path) `/v1/wallets/${walletID}/raw_sign`, {
            body,
            ...options,
            headers: (0, headers_1.buildHeaders)([
                {
                    ...(privyAuthorizationSignature != null ?
                        { 'privy-authorization-signature': privyAuthorizationSignature }
                        : undefined),
                    ...(privyIdempotencyKey != null ? { 'privy-idempotency-key': privyIdempotencyKey } : undefined),
                    ...(privyRequestExpiry != null ? { 'privy-request-expiry': privyRequestExpiry } : undefined),
                },
                options?.headers,
            ]),
        });
    }
    /**
     * Sign a message or transaction with a wallet by wallet ID.
     *
     * @example
     * ```ts
     * const walletRpcResponse = await client.wallets._rpc(
     *   'wallet_id',
     *   {
     *     method: 'eth_sendTransaction',
     *     params: {
     *       transaction: {
     *         to: '0x0000000000000000000000000000000000000000',
     *         value: 1,
     *       },
     *     },
     *     chain_type: 'ethereum',
     *   },
     * );
     * ```
     */
    _rpc(walletID, params, options) {
        const { 'privy-authorization-signature': privyAuthorizationSignature, 'privy-idempotency-key': privyIdempotencyKey, 'privy-request-expiry': privyRequestExpiry, ...body } = params;
        return this._client.post((0, path_1.path) `/v1/wallets/${walletID}/rpc`, {
            body,
            ...options,
            headers: (0, headers_1.buildHeaders)([
                {
                    ...(privyAuthorizationSignature != null ?
                        { 'privy-authorization-signature': privyAuthorizationSignature }
                        : undefined),
                    ...(privyIdempotencyKey != null ? { 'privy-idempotency-key': privyIdempotencyKey } : undefined),
                    ...(privyRequestExpiry != null ? { 'privy-request-expiry': privyRequestExpiry } : undefined),
                },
                options?.headers,
            ]),
        });
    }
    /**
     * Submit a wallet import request.
     *
     * @example
     * ```ts
     * const wallet = await client.wallets._submitImport({
     *   wallet: {
     *     address: '0xF1DBff66C993EE895C8cb176c30b07A559d76496',
     *     chain_type: 'ethereum',
     *     ciphertext:
     *       'PRoRXygG+YYSDBXjCopNYZmx8Z6nvdl1D0lpePTYZdZI2VGfK+LkFt+GlEJqdoi9',
     *     encapsulated_key:
     *       'BOhR6xITDt5THJawHHJKrKdI9CBr2M/SDWzZZAaOW4gCMsSpC65U007WyKiwuuOVAo1BNm4YgcBBROuMmyIZXZk=',
     *     encryption_type: 'HPKE',
     *     entropy_type: 'private-key',
     *   },
     * });
     * ```
     */
    _submitImport(body, options) {
        return this._client.post('/v1/wallets/import/submit', { body, ...options });
    }
    /**
     * Transfer tokens from a wallet to a destination address.
     *
     * @example
     * ```ts
     * const transferActionResponse =
     *   await client.wallets._transfer('wallet_id', {
     *     destination: {
     *       address: '0xB00F0759DbeeF5E543Cc3E3B07A6442F5f3928a2',
     *       asset: 'usdc',
     *       chain: 'base',
     *     },
     *     source: {
     *       amount: '10.5',
     *       asset: 'usdc',
     *       chain: 'base',
     *     },
     *     amount_type: 'exact_input',
     *     slippage_bps: 100,
     *   });
     * ```
     */
    _transfer(walletID, params, options) {
        const { 'privy-authorization-signature': privyAuthorizationSignature, 'privy-idempotency-key': privyIdempotencyKey, 'privy-request-expiry': privyRequestExpiry, ...body } = params;
        return this._client.post((0, path_1.path) `/v1/wallets/${walletID}/transfer`, {
            body,
            ...options,
            headers: (0, headers_1.buildHeaders)([
                {
                    ...(privyAuthorizationSignature != null ?
                        { 'privy-authorization-signature': privyAuthorizationSignature }
                        : undefined),
                    ...(privyIdempotencyKey != null ? { 'privy-idempotency-key': privyIdempotencyKey } : undefined),
                    ...(privyRequestExpiry != null ? { 'privy-request-expiry': privyRequestExpiry } : undefined),
                },
                options?.headers,
            ]),
        });
    }
    /**
     * Update a wallet's policies or authorization key configuration.
     *
     * @example
     * ```ts
     * const wallet = await client.wallets._update('wallet_id', {
     *   policy_ids: ['tb54eps4z44ed0jepousxi4n'],
     * });
     * ```
     */
    _update(walletID, params, options) {
        const { 'privy-authorization-signature': privyAuthorizationSignature, 'privy-request-expiry': privyRequestExpiry, ...body } = params;
        return this._client.patch((0, path_1.path) `/v1/wallets/${walletID}`, {
            body,
            ...options,
            headers: (0, headers_1.buildHeaders)([
                {
                    ...(privyAuthorizationSignature != null ?
                        { 'privy-authorization-signature': privyAuthorizationSignature }
                        : undefined),
                    ...(privyRequestExpiry != null ? { 'privy-request-expiry': privyRequestExpiry } : undefined),
                },
                options?.headers,
            ]),
        });
    }
    /**
     * Archives a wallet, preventing it from being used in any write or signing
     * operations. Archived wallets are hidden from list endpoints by default. Returns
     * 404 if the wallet does not exist or is already archived.
     *
     * @example
     * ```ts
     * const wallet = await client.wallets.archive('wallet_id');
     * ```
     */
    archive(walletID, options) {
        return this._client.post((0, path_1.path) `/v1/wallets/${walletID}/archive`, options);
    }
    /**
     * Exchange a user JWT for a session key authorized to act on the user's wallets.
     * Returns the encrypted authorization key and the list of wallets it can access.
     *
     * @example
     * ```ts
     * const walletAuthenticateWithJwtResponse =
     *   await client.wallets.authenticateWithJwt({
     *     encryption_type: 'HPKE',
     *     recipient_public_key:
     *       'DAQcDQgAEx4aoeD72yykviK+fckqE2CItVIGn1rCnvCXZ1HgpOcMEMialRmTrqIK4oZlYd1',
     *     user_jwt:
     *       'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiYWRtaW4iOnRydWUsImlhdCI6MTUxNjIzOTAyMn0.KMUFsIDTnFmyG3nMiGM6H9FNFUROf3wh7SmqJp-QV30',
     *   });
     * ```
     */
    authenticateWithJwt(body, options) {
        return this._client.post('/v1/wallets/authenticate', { body, ...options });
    }
    /**
     * Creates multiple wallets in a single request. Each wallet creation is
     * independent; failures for one wallet do not affect others. Maximum batch size is
     * 100 wallets.
     *
     * @example
     * ```ts
     * const walletBatchCreateResponse =
     *   await client.wallets.createBatch({
     *     wallets: [
     *       { chain_type: 'ethereum' },
     *       { chain_type: 'solana' },
     *     ],
     *   });
     * ```
     */
    createBatch(body, options) {
        return this._client.post('/v1/wallets/batch', { body, ...options });
    }
    /**
     * Create one or more wallets associated with a recovery user, so the user can
     * later regain wallet access via the linked accounts. Deprecated; prefer the
     * standard wallet creation flow combined with a separate recovery setup.
     *
     * @deprecated
     */
    createWalletsWithRecovery(body, options) {
        return this._client.post('/v1/wallets_with_recovery', { body, ...options });
    }
    /**
     * Get a wallet by wallet ID.
     *
     * @example
     * ```ts
     * const wallet = await client.wallets.get('wallet_id');
     * ```
     */
    get(walletID, query = {}, options) {
        if (walletID === '') {
            throw new Error('walletID must not be an empty string');
        }
        return this._client.get((0, path_1.path) `/v1/wallets/${walletID}`, { query, ...options });
    }
    /**
     * Look up a wallet by its blockchain address. Returns the wallet object if found.
     *
     * @example
     * ```ts
     * const wallet = await client.wallets.getWalletByAddress({
     *   address: '0xF1DBff66C993EE895C8cb176c30b07A559d76496',
     * });
     * ```
     */
    getWalletByAddress(body, options) {
        return this._client.post('/v1/wallets/address', { body, ...options });
    }
}
exports.Wallets = Wallets;
Wallets.Actions = actions_1.Actions;
Wallets.Earn = earn_1.Earn;
Wallets.Transactions = transactions_1.Transactions;
Wallets.Balance = balance_1.Balance;
Wallets.Swap = swap_1.Swap;
//# sourceMappingURL=wallets.js.map