import { Incentive } from "../../../resources/wallets/earn/ethereum/incentive.mjs";
import { prepareRequest } from "../../../lib/authorization.mjs";
export class PrivyEarnEthereumIncentiveService extends Incentive {
    constructor(privyApiClient, privyClient) {
        super(privyApiClient);
        this.privyClient = privyClient;
    }
    async claim(walletId, { authorization_context: authorizationContext = {}, idempotency_key: idempotencyKey, ...params }) {
        const { headers } = await prepareRequest(this.privyClient, this._client.appID, {
            authorizationContext,
            idempotencyKey,
            requestExpiry: this.privyClient.getRequestExpiry(),
            method: 'POST',
            url: `${this._client.baseURL}/v1/wallets/${walletId}/earn/ethereum/incentive/claim`,
            body: params,
        });
        return await this._claim(walletId, { ...params, ...headers });
    }
}
//# sourceMappingURL=ethereum-incentive.mjs.map