"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PrivyEarnEthereumService = void 0;
const ethereum_1 = require("../../../resources/wallets/earn/ethereum/ethereum.js");
const authorization_1 = require("../../../lib/authorization.js");
const ethereum_incentive_1 = require("./ethereum-incentive.js");
class PrivyEarnEthereumService extends ethereum_1.Ethereum {
    constructor(privyApiClient, privyClient) {
        super(privyApiClient);
        this.privyClient = privyClient;
        this.incentiveService = new ethereum_incentive_1.PrivyEarnEthereumIncentiveService(privyApiClient, privyClient);
    }
    incentive() {
        return this.incentiveService;
    }
    async deposit(walletId, { authorization_context: authorizationContext = {}, idempotency_key: idempotencyKey, ...params }) {
        const { headers } = await (0, authorization_1.prepareRequest)(this.privyClient, this._client.appID, {
            authorizationContext,
            idempotencyKey,
            requestExpiry: this.privyClient.getRequestExpiry(),
            method: 'POST',
            url: `${this._client.baseURL}/v1/wallets/${walletId}/earn/ethereum/deposit`,
            body: params,
        });
        return await this._deposit(walletId, { ...params, ...headers });
    }
    async withdraw(walletId, { authorization_context: authorizationContext = {}, idempotency_key: idempotencyKey, ...params }) {
        const { headers } = await (0, authorization_1.prepareRequest)(this.privyClient, this._client.appID, {
            authorizationContext,
            idempotencyKey,
            requestExpiry: this.privyClient.getRequestExpiry(),
            method: 'POST',
            url: `${this._client.baseURL}/v1/wallets/${walletId}/earn/ethereum/withdraw`,
            body: params,
        });
        return await this._withdraw(walletId, { ...params, ...headers });
    }
}
exports.PrivyEarnEthereumService = PrivyEarnEthereumService;
//# sourceMappingURL=ethereum.js.map