import type { Address } from "viem";
interface Account {
    address: Address;
}
export type AccountParam = Address | Account | null;
export declare function resolveAddress(account: Address | Account): Address;
export {};
