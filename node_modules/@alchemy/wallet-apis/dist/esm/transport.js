import { alchemyTransport, } from "@alchemy/common";
export const alchemyWalletTransport = (config) => {
    return alchemyTransport({
        ...config,
        url: config.url ?? "https://api.g.alchemy.com/v2",
    });
};
//# sourceMappingURL=transport.js.map