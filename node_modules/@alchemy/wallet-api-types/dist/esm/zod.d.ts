import * as z from "zod";
export { z };
/** Shared options bag mirroring TypeBox's `SchemaOptions`, used by all schema helpers in this module. */
export type SchemaOptions = {
    title?: string;
    description?: string;
    default?: unknown;
    error?: string;
    errorMessage?: string;
    excludeFromApiSpecGeneration?: boolean;
};
/** Resolves the validation error message from either `error` or `errorMessage` option. */
export declare const errorMessage: (options?: SchemaOptions) => string | undefined;
/** Attaches non-undefined {@link SchemaOptions} fields to a schema via `.meta()`. */
export declare function withMetadata<T extends z.ZodType>(schema: T, options?: SchemaOptions): T;
/** Applies {@link SchemaOptions} metadata to an object schema. */
export declare function withObjectOptions<T extends z.ZodObject>(schema: T, options?: SchemaOptions): T;
/** Drop-in replacement for `z.object()` that accepts {@link SchemaOptions} for metadata and validation. */
export declare function object<T extends z.ZodRawShape>(shape: T, options?: SchemaOptions): z.ZodObject<{ -readonly [P in keyof T]: T[P]; }, z.core.$strip>;
/** Like {@link object} but uses `z.strictObject()` — rejects unknown keys. Marks the schema with `x-strictObject` metadata for the OpenRPC generator. */
export declare function strictObject<T extends z.ZodRawShape>(shape: T, options?: SchemaOptions): z.ZodObject<{ -readonly [P in keyof T]: T[P]; }, z.core.$strict>;
/** Drop-in replacement for `z.union()` with options-first arg order (matches TypeBox's `Type.Union`). */
export declare function union<T extends readonly [z.ZodType, z.ZodType, ...z.ZodType[]]>(options: SchemaOptions | undefined, schemas: T): z.ZodUnion<T>;
